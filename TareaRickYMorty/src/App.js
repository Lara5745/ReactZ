/*Este archivo se encarga de retornar lo que será renderizado en index.js #5*/


//Jerarquía de IMPORTS

import React from "react"; //Componentes por defecto de react

import "semantic-ui-css/semantic.min.css"; //Dependencias/librerias

import Layout from "./Components/Layout"; //Componentes propios

export const App = () => {
  return <Layout />;
};
